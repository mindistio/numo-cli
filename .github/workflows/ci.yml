name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build
        env:
          NUMO_FIREBASE_API_KEY: ${{ secrets.NUMO_FIREBASE_API_KEY }}
          NUMO_FIREBASE_PROJECT_ID: ${{ secrets.NUMO_FIREBASE_PROJECT_ID }}
          NUMO_FIREBASE_APP_ID: ${{ secrets.NUMO_FIREBASE_APP_ID }}
          NUMO_ADMIN_UIDS: ${{ secrets.NUMO_ADMIN_UIDS }}

      - name: Verify binary runs
        run: node dist/cli.cjs --version

      - name: Test
        run: npm test
